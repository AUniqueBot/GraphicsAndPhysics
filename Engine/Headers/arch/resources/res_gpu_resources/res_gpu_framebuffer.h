#pragma once
#include <pch.h>

namespace FramebufferConstants {
	inline constexpr const GLuint C_FRAMEBUFFER_COLOR_OUT = 0;
	inline constexpr const GLuint C_FRAMEBUFFER_ID_OUT = 31;	// ID should change to something else
}