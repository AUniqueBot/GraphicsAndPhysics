#pragma once
#include <pch.h>
#include <arch/ecs/ecs_entityFactory.h>

namespace Serialization {
	class Reader {
	public:
		Reader(std::filesystem::path _path);
	private:
		// some sort of data here.
	};
}