#include <pch.h>
#include <arch/resources/res_texture.h>




bool Texture::Load() {
	
	//glGenTextures(1, &m_textureID);
	//glBindTexture(GL_TEXTURE_2D, m_textureID);


	//// on load set defaults.
	//unsigned char* imgData = stbi_load(_path.string().c_str(), &m_width, &m_height, &m_channelCount, 0);

	//if (!imgData) {
	//	std::cout << "TEXTURE NAME || [" << _path.filename() << "]: FAILED TO LOAD" << std::endl;
	//}
	//else {	
	//	glTexImage2D(GL_TEXTURE_2D, 0, GL_RGB, m_width, m_height, 0, GL_RGB, GL_UNSIGNED_BYTE, imgData);
	//	glGenerateMipmap(GL_TEXTURE_2D);
	//}


	//stbi_image_free(imgData);
	

	// 



	return false;
}
